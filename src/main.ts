const app = document.querySelector<HTMLDivElement>("#app");

if (app) {
  app.innerHTML = `
    <div>
      <h1>Draftline</h1>
      <p>Scaffold running. The compare UI lands in the next build pass.</p>
    </div>
  `;
}
